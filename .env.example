# Copy to .env locally, or set these in the Vercel project → Settings → Environment Variables.
# Never commit a real .env file.

DATABASE_URL=
BETTER_AUTH_SECRET=
BETTER_AUTH_URL=https://www.dogtrainingwithemily.com
VITE_AUTH_ENABLED=true
